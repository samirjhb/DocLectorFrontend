export const environment = {
  production: true,
  // Reemplazar por la URL real del backend en Render después del primer deploy
  // (p.ej. "https://claridad-contrato-api.onrender.com/api") y volver a hacer commit/push.
  apiUrl: 'https://claridad-contrato-api.onrender.com/api',
};
